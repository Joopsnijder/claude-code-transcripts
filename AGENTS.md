# Agent Instructions

## Type Checking

Use **pyright** for type checking:

```bash
pyright
```

## Testing

Use **pytest-cov** with a minimum coverage of 80%:

```bash
pytest --cov=. --cov-fail-under=80
```
